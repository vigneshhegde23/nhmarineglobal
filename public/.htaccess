Options -MultiViews
RewriteEngine On
RewriteBase /

# Let existing files and directories load normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route all other requests through the React app entry point.
RewriteRule ^ index.html [L]
